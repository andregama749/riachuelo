import { FadeFabDirective } from './fade-fab.directive';

describe('FadeFabDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeFabDirective();
    expect(directive).toBeTruthy();
  });
});
